services:
  react-native:
    build:
      context: .
      dockerfile: ./Dockerfile
    image: react-native
    container_name: react-native
    ports:
      - 8081:8081
    volumes:
      - ./workspace:/workspace
    tty: true
